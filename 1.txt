咱们来优化一下流程：1:当我发给你base:app_id的时候，证明vercel.txt里面的vercel token就是我新加入的token，在你将base:app_id填入vercel.txt的位置还有.env.local文件的位置和一些工作后。你使用这个vercel token在当前项目文件夹的命令行vercel link --token=当前token，通过这个新加的token将当前项目链接下vercel，并且vercel --prod --token=当前token，prod下项目，获取项目的url，然后将rul替换掉vercel.txt里面的Deployed URL。（以后我都会在vercrl.txt里面添加了钱包和密钥）你接着将密钥剪切到项目文件夹里面的.env.local文件里的DEPLOYER_PRIVATE_KEY=密钥位置。然后在当前项目的文件夹命令行里npm run deploy:contract,获取项目的合同，并把合同地址填入.env.local文件里额NEXT_PUBLIC_SKILL_MENU_CONTRACT_ADDRESS=这里。和vercel.txt文件里面的eplace_with_skill_menu_contract_address位置。2:当我发给你builld code的时候，你将builld code的工作做完后，要将它填入vercel.txt和.env.local的所需位置，填写完成后请用当前vercel token帮我提交下项目所需的一些信息到vercelprintf "6a097e421b76c7abf6d06a94" | vercel env add NEXT_PUBLIC_BASE_APP_ID production --token=token
printf "replace_with_builder_code" | vercel env add NEXT_PUBLIC_BUILDER_CODE production --token=token
printf "replace_with_skill_menu_contract_address" | vercel env add NEXT_PUBLIC_SKILL_MENU_CONTRACT_ADDRESS production --token=token，这里的token需要帮我填入当前的vercel的token。然后再次vercel --prod --token=当前token，prod下项目，完成后告诉我完成了。注意，每个流程都要显示并告诉我。